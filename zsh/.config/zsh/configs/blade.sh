# Frum Ruby version manager
# For zsh, you would use:
# if command -v frum &> /dev/null
# then
#     eval "$(frum init --shell zsh)"
#     eval "$(frum completions --shell zsh)"
# fi

# source ~/.venv/bin/activate
